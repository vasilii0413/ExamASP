@model IEnumerable<CarRental.Models.TypeOfDriving>

@{
    ViewData["Title"] = "Drive Types";
}

<h1>Drive Types</h1>

<div class="text-right">
    <a asp-action="Create" class="btn btn-primary">Create New</a>
</div>
<table class="table table-hover">
    <thead>
        <tr>
            <th>
                @*@Html.DisplayNameFor(model => model.TypeDrive)*@
            </th>
            <th></th>
        </tr>
    </thead>
    <tbody>
@foreach (var item in Model) {
        <tr class="table-primary">
                <td class="blockquote">
                @Html.DisplayFor(modelItem => item.DriveTypeId)
                @Html.DisplayFor(modelItem => item.TypeDrive)
            </td>
            <td>
                <div class="btn-group">
                <a asp-action="Edit" asp-route-id="@item.DriveTypeId"class="btn btn-primary">Edit</a> 
                @*<a asp-action="Details" asp-route-id="@item.DriveTypeId">Details</a>*@ 
                <a asp-action="Delete" asp-route-id="@item.DriveTypeId"class="btn btn-danger">Delete</a>
                    </div>
            </td>
        </tr>
}
    </tbody>
</table>
